apiVersion: v1
kind: Service

metadata:
  name: twenty-crm-service
  labels:
    app: twenty-crm

spec:
  selector:
    app: twenty-crm

  ports:
    - name: http
      protocol: TCP
      port: 2020
      targetPort: 2020
      nodePort: 30220

  type: NodePort