apiVersion: v1
kind: Service
metadata:
  name: twenty-crm-service
  labels:
    app: twenty-crm
spec:
  type: NodePort
  selector:
    app: twenty-crm
  ports:
    - name: http
      port: 2020
      targetPort: 2020
      nodePort: 30020
