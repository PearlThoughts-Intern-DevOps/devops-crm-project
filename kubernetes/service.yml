apiVersion: v1
kind: Service
metadata:
  name: twenty-crm-service
  namespace: twenty-crm
spec:
  type: LoadBalancer
  selector:
    app: twenty-crm
  ports:
    - protocol: TCP
      port: 2020
      targetPort: 3000