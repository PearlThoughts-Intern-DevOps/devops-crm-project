apiVersion: apps/v1
kind: Deployment
metadata:
  name: twenty-postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: twenty-postgres
  template:
    metadata:
      labels:
        app: twenty-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              value: postgres
            - name: POSTGRES_PASSWORD
              value: postgres
            - name: POSTGRES_DB
              value: twenty
---
apiVersion: v1
kind: Service
metadata:
  name: twenty-postgres
spec:
  selector:
    app: twenty-postgres
  ports:
    - port: 5432
      targetPort: 5432