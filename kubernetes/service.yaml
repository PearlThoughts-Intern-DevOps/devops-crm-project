apiVersion: v1
kind: Service
metadata:
  name: twenty-crm
spec:
  selector:
    app: twenty-crm
  type: NodePort
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30080